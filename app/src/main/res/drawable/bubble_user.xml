<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Bubble color -->
    <solid android:color="#6A0DAD" /> <!-- purple, match your brand -->

    <!-- Rounded corners -->
    <corners
        android:radius="20dp" />

    <!-- Optional subtle shadow -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>
